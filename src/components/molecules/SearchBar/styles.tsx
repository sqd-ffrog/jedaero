import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  cardStyle: {
    flexDirection: "row",
    alignItems: "center"
  },
  inputStyle: {
    flex: 1
  },
  closeStyle: {
    marginHorizontal: 4
  }
});

export default styles;
